import styled from 'styled-components/native';

const S = {
    Container: styled.View`
        flex: 1;
        justify-content: center;
        align-items: center;
    `,

    Title: styled.Text`
        color: red;
    `    
};

export default S;